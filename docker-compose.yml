services:
  spots:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - spots-data:/app/data
    environment:
      - ConnectionStrings__DefaultConnection=Data Source=/app/data/spots.db

volumes:
  spots-data:
